spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/karma_db
    username: user
    password: password
  jpa:
    hibernate:
      ddl-auto: validate
    database: postgresql
  flyway:
    enabled: true
