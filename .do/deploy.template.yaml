spec:
  name: karma-volunteer-platform
  services:
    - name: api
      git:
        branch: main
        repo_clone_url: https://github.com/jshimas/karma-volunteer-platform.git
      source_dir: karma-api
      dockerfile_path: karma-api/Dockerfile
      routes:
        - path: /karma-api

